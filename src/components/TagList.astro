---
/** Row of technology tags. */
import './TagList.css';

interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<div class="tags">
  {tags.map((tag) => <span class="tag">{tag}</span>)}
</div>
